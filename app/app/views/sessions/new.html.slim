section.new
  .new__heading
    h2.new__heading__text
      | Login
  - if @error_message
    ul.errors__messages
      li
        = @error_message
  = form_with  url: sessions_path, class: "form_class", local: true do |f|
    = f.label :email, 'メールアドレス'
    = f.email_field :email
    = f.label :password, 'パスワード'
    = f.password_field :password
    = f.submit "ログイン", class: "submit"
